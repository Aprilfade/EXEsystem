# 管理端课程管理全面优化方案 - v3.07

## 一、现状分析

### 1.1 学生端已实现功能（参考基准）

✅ **课程学习中心**（v3.06）
- 章节树导航（无限层级，递归展示）
- 视频播放器（DPlayer，倍速、清晰度、断点续播）
- 文档查看器（PDF/PPT在线预览）
- 学习进度追踪（自动保存，进度条显示）
- 学习数据分析（ECharts图表，学习统计）
- 讨论评论区
- 现代化UI（卡片、动画、骨架屏）

### 1.2 管理端现状

✅ **已有功能**
- 课程CRUD（创建、编辑、删除）
- 资源管理（上传、删除、预览）
- 资源类型支持（VIDEO/PDF/PPT/LINK）
- 知识点关联

❌ **缺失功能**
- 章节管理UI（虽然后端已有完整的Service）
- 资源无法分配到章节（chapter_id未使用）
- 无法查看学生学习数据
- 无章节树可视化
- 无拖拽排序功能

### 1.3 数据库支持

✅ **已完成数据库设计**
- `biz_course_chapter` - 章节表（支持无限层级）
- `biz_course_progress` - 学习进度表
- `biz_study_session` - 学习会话表
- `biz_course_resource.chapter_id` - 资源关联字段

✅ **后端服务层**
- `CourseChapterService` - 章节CRUD服务
- `CourseProgressService` - 进度查询服务
- `CourseChapterController` - 需要创建（管理端API）

---

## 二、优化目标

### 核心目标
将管理端课程管理提升到与学生端学习中心相匹配的水平，实现完整的课程内容编排能力。

### 功能对齐
| 功能 | 学生端 | 管理端现状 | 优化目标 |
|------|--------|-----------|---------|
| 章节管理 | ✅ 查看章节树 | ❌ 无UI | ✅ 创建/编辑/删除章节 |
| 资源分配 | ✅ 按章节浏览 | ⚠️ 扁平列表 | ✅ 分配到章节 |
| 排序功能 | ✅ 自动排序 | ⚠️ 简单排序 | ✅ 拖拽排序 |
| 学习数据 | ✅ 个人数据 | ❌ 无法查看 | ✅ 查看全班数据 |
| 树形结构 | ✅ 递归展示 | ❌ 无 | ✅ 树形管理 |

---

## 三、技术架构设计

### 3.1 后端新增API

#### 管理端章节API（新建Controller）

**文件**: `exe-backend/src/main/java/com/ice/exebackend/controller/admin/CourseChapterAdminController.java`

```java
@RestController
@RequestMapping("/api/v1/admin/course-chapter")
@PreAuthorize("hasAnyAuthority('ROLE_ADMIN', 'ROLE_TEACHER')")
public class CourseChapterAdminController {

    /**
     * 获取课程章节树（管理员视图，含资源）
     */
    @GetMapping("/course/{courseId}/tree")
    public Result getChapterTree(@PathVariable Long courseId) {}

    /**
     * 创建章节
     */
    @PostMapping
    public Result createChapter(@RequestBody BizCourseChapter chapter) {}

    /**
     * 更新章节
     */
    @PutMapping("/{id}")
    public Result updateChapter(@PathVariable Long id, @RequestBody BizCourseChapter chapter) {}

    /**
     * 删除章节（级联删除子章节，资源chapter_id置null）
     */
    @DeleteMapping("/{id}")
    public Result deleteChapter(@PathVariable Long id) {}

    /**
     * 批量更新章节排序
     */
    @PostMapping("/batch-sort")
    public Result batchUpdateSort(@RequestBody List<ChapterSortDTO> sortList) {}

    /**
     * 移动章节（修改parent_id）
     */
    @PostMapping("/{id}/move")
    public Result moveChapter(@PathVariable Long id, @RequestParam Long newParentId) {}
}
```

#### 学习数据查看API（扩展现有Controller）

**文件**: `exe-backend/src/main/java/com/ice/exebackend/controller/admin/CourseProgressAdminController.java`

```java
@RestController
@RequestMapping("/api/v1/admin/course-progress")
@PreAuthorize("hasAnyAuthority('ROLE_ADMIN', 'ROLE_TEACHER')")
public class CourseProgressAdminController {

    /**
     * 获取课程学习概览（班级维度）
     */
    @GetMapping("/course/{courseId}/overview")
    public Result getCourseProgressOverview(@PathVariable Long courseId) {
        // 返回：总学生数、完成人数、平均进度、平均学习时长
    }

    /**
     * 获取课程学习详情列表（学生维度）
     */
    @GetMapping("/course/{courseId}/students")
    public Result getCourseStudentProgress(
        @PathVariable Long courseId,
        @RequestParam(defaultValue = "1") int current,
        @RequestParam(defaultValue = "10") int size
    ) {
        // 返回分页列表：学生姓名、完成率、学习时长、最后学习时间
    }

    /**
     * 获取学生详细学习进度（资源维度）
     */
    @GetMapping("/student/{studentId}/course/{courseId}")
    public Result getStudentDetailProgress(
        @PathVariable Long studentId,
        @PathVariable Long courseId
    ) {
        // 返回：每个资源的学习进度、完成状态、学习时长
    }

    /**
     * 导出课程学习数据（Excel）
     */
    @GetMapping("/course/{courseId}/export")
    public void exportCourseProgress(@PathVariable Long courseId, HttpServletResponse response) {
        // 导出Excel：学生列表、进度、时长等
    }
}
```

### 3.2 前端组件架构

#### 核心组件结构

```
exe-frontend/src/views/
├── CourseManage.vue (主页面 - 需要重构)
└── course-admin/
    ├── ChapterManager.vue          (章节管理主组件 ~500行)
    ├── ChapterTreeEditor.vue       (章节树编辑器 ~400行)
    ├── ChapterFormDialog.vue       (章节表单弹窗 ~200行)
    ├── ResourceAssignment.vue      (资源分配组件 ~300行)
    ├── CourseProgressDashboard.vue (学习数据看板 ~600行)
    └── hooks/
        ├── useChapterTree.ts       (章节树操作)
        ├── useDragSort.ts          (拖拽排序)
        └── useCourseProgress.ts    (学习数据统计)
```

#### API接口定义

**文件**: `exe-frontend/src/api/courseAdmin.ts`

```typescript
/**
 * 管理端课程章节API
 */

export interface ChapterNode {
  id: number;
  courseId: number;
  parentId: number;
  name: string;
  description?: string;
  sortOrder: number;
  children?: ChapterNode[];
  resources?: CourseResource[];
}

export interface ChapterSortDTO {
  id: number;
  sortOrder: number;
}

export interface CourseProgressOverview {
  totalStudents: number;
  completedStudents: number;
  averageProgress: number;
  averageStudyTime: number;
}

export interface StudentProgressItem {
  studentId: number;
  studentName: string;
  studentNo: string;
  completionRate: number;
  totalStudyTime: number;
  lastStudyTime: string;
}

// API方法
export function getChapterTree(courseId: number): Promise<ApiResult<ChapterNode[]>> {
  return request({ url: `/api/v1/admin/course-chapter/course/${courseId}/tree`, method: 'get' });
}

export function createChapter(data: Partial<ChapterNode>): Promise<ApiResult<number>> {
  return request({ url: '/api/v1/admin/course-chapter', method: 'post', data });
}

export function updateChapter(id: number, data: Partial<ChapterNode>): Promise<ApiResult<string>> {
  return request({ url: `/api/v1/admin/course-chapter/${id}`, method: 'put', data });
}

export function deleteChapter(id: number): Promise<ApiResult<string>> {
  return request({ url: `/api/v1/admin/course-chapter/${id}`, method: 'delete' });
}

export function batchUpdateChapterSort(sortList: ChapterSortDTO[]): Promise<ApiResult<string>> {
  return request({ url: '/api/v1/admin/course-chapter/batch-sort', method: 'post', data: sortList });
}

export function getCourseProgressOverview(courseId: number): Promise<ApiResult<CourseProgressOverview>> {
  return request({ url: `/api/v1/admin/course-progress/course/${courseId}/overview`, method: 'get' });
}

export function getCourseStudentProgress(
  courseId: number,
  current: number,
  size: number
): Promise<ApiResult<Page<StudentProgressItem>>> {
  return request({
    url: `/api/v1/admin/course-progress/course/${courseId}/students`,
    method: 'get',
    params: { current, size }
  });
}
```

---

## 四、核心功能设计

### 4.1 章节树管理器（ChapterTreeEditor.vue）

#### 功能特性
1. **树形展示**
   - Element Plus `el-tree` 组件
   - 支持无限层级嵌套
   - 可折叠/展开节点
   - 图标区分章节/资源

2. **拖拽排序**
   - 支持同级拖拽排序
   - 支持跨级拖拽移动
   - 拖拽时实时预览
   - 拖拽后自动保存

3. **右键菜单**
   - 添加子章节
   - 编辑章节
   - 删除章节
   - 添加资源

4. **批量操作**
   - 批量移动资源
   - 批量删除
   - 批量导出

#### 核心代码示例

```vue
<template>
  <div class="chapter-tree-editor">
    <!-- 工具栏 -->
    <div class="toolbar">
      <el-button type="primary" @click="handleAddRootChapter">
        <el-icon><Plus /></el-icon>
        添加根章节
      </el-button>
      <el-button @click="expandAll">全部展开</el-button>
      <el-button @click="collapseAll">全部折叠</el-button>
    </div>

    <!-- 章节树 -->
    <el-tree
      ref="treeRef"
      :data="chapterTree"
      :props="treeProps"
      node-key="id"
      draggable
      :allow-drop="allowDrop"
      :allow-drag="allowDrag"
      @node-drop="handleNodeDrop"
      @node-contextmenu="handleContextMenu"
    >
      <template #default="{ node, data }">
        <div class="tree-node">
          <el-icon v-if="data.type === 'chapter'"><Folder /></el-icon>
          <el-icon v-else><Document /></el-icon>
          <span class="node-label">{{ data.name }}</span>
          <span class="node-actions">
            <el-button
              v-if="data.type === 'chapter'"
              link
              size="small"
              @click.stop="handleAddChild(data)"
            >
              <el-icon><Plus /></el-icon>
            </el-button>
            <el-button link size="small" @click.stop="handleEdit(data)">
              <el-icon><Edit /></el-icon>
            </el-button>
            <el-button link size="small" type="danger" @click.stop="handleDelete(data)">
              <el-icon><Delete /></el-icon>
            </el-button>
          </span>
        </div>
      </template>
    </el-tree>

    <!-- 章节表单弹窗 -->
    <ChapterFormDialog
      v-model:visible="dialogVisible"
      :chapter="currentChapter"
      :parent-id="currentParentId"
      @success="refreshTree"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';
import { ElTree, ElMessage } from 'element-plus';
import { Plus, Folder, Document, Edit, Delete } from '@element-plus/icons-vue';
import { getChapterTree, batchUpdateChapterSort } from '@/api/courseAdmin';
import ChapterFormDialog from './ChapterFormDialog.vue';
import type { ChapterNode } from '@/api/courseAdmin';

const props = defineProps<{
  courseId: number;
}>();

const treeRef = ref<InstanceType<typeof ElTree>>();
const chapterTree = ref<ChapterNode[]>([]);
const dialogVisible = ref(false);
const currentChapter = ref<ChapterNode | null>(null);
const currentParentId = ref<number>(0);

const treeProps = {
  children: 'children',
  label: 'name',
  disabled: (data: any) => data.type === 'resource'  // 资源节点不可拖拽
};

// 加载章节树
const loadTree = async () => {
  const res = await getChapterTree(props.courseId);
  if (res.code === 200) {
    chapterTree.value = res.data;
  }
};

// 拖拽控制
const allowDrag = (node: any) => {
  return node.data.type === 'chapter';  // 只允许章节拖拽
};

const allowDrop = (draggingNode: any, dropNode: any, type: string) => {
  // 不允许资源作为父节点
  if (type === 'inner' && dropNode.data.type === 'resource') {
    return false;
  }
  return true;
};

// 拖拽完成处理
const handleNodeDrop = async (draggingNode: any, dropNode: any, dropType: string) => {
  // 收集新的排序数据
  const sortList: any[] = [];
  const collectSort = (nodes: any[], parentId: number = 0) => {
    nodes.forEach((node, index) => {
      if (node.type === 'chapter') {
        sortList.push({
          id: node.id,
          sortOrder: index,
          parentId: parentId
        });
        if (node.children) {
          collectSort(node.children, node.id);
        }
      }
    });
  };

  collectSort(chapterTree.value);

  // 批量更新排序
  await batchUpdateChapterSort(sortList);
  ElMessage.success('章节顺序已更新');
  loadTree();
};

// 添加根章节
const handleAddRootChapter = () => {
  currentChapter.value = null;
  currentParentId.value = 0;
  dialogVisible.value = true;
};

// 添加子章节
const handleAddChild = (data: ChapterNode) => {
  currentChapter.value = null;
  currentParentId.value = data.id;
  dialogVisible.value = true;
};

// 编辑章节
const handleEdit = (data: ChapterNode) => {
  currentChapter.value = data;
  currentParentId.value = data.parentId;
  dialogVisible.value = true;
};

// 删除章节
const handleDelete = async (data: ChapterNode) => {
  await ElMessageBox.confirm(
    '删除章节将同时删除其子章节，章节下的资源将变为未分类资源，确定删除吗？',
    '确认删除',
    { type: 'warning' }
  );

  const res = await deleteChapter(data.id);
  if (res.code === 200) {
    ElMessage.success('删除成功');
    loadTree();
  }
};

// 刷新树
const refreshTree = () => {
  loadTree();
};

// 全部展开/折叠
const expandAll = () => {
  const nodes = treeRef.value?.store.nodesMap;
  for (let key in nodes) {
    nodes[key].expanded = true;
  }
};

const collapseAll = () => {
  const nodes = treeRef.value?.store.nodesMap;
  for (let key in nodes) {
    nodes[key].expanded = false;
  }
};

// 初始化
loadTree();
</script>

<style scoped lang="scss">
.chapter-tree-editor {
  height: 100%;
  display: flex;
  flex-direction: column;

  .toolbar {
    padding: 16px;
    border-bottom: 1px solid #e4e7ed;
  }

  .el-tree {
    flex: 1;
    overflow-y: auto;
    padding: 16px;
  }

  .tree-node {
    display: flex;
    align-items: center;
    width: 100%;
    padding: 8px;

    .el-icon {
      margin-right: 8px;
      font-size: 16px;
    }

    .node-label {
      flex: 1;
    }

    .node-actions {
      display: none;
    }

    &:hover .node-actions {
      display: block;
    }
  }
}
</style>
```

### 4.2 资源分配组件（ResourceAssignment.vue）

#### 功能特性
1. **资源上传时选择章节**
   - 下拉选择章节（树形选择器）
   - 支持创建新章节
   - 默认选中最后使用的章节

2. **批量分配资源到章节**
   - 勾选多个资源
   - 批量移动到指定章节
   - 批量移除章节归属

3. **未分类资源管理**
   - 单独展示未分配章节的资源（`chapter_id IS NULL`）
   - 一键分配功能

#### 核心代码示例

```vue
<template>
  <div class="resource-assignment">
    <!-- 章节选择器 -->
    <el-form-item label="所属章节">
      <el-tree-select
        v-model="form.chapterId"
        :data="chapterOptions"
        :props="{ label: 'name', value: 'id', children: 'children' }"
        placeholder="请选择章节（可选）"
        clearable
        check-strictly
      />
      <el-button link type="primary" @click="handleCreateChapter">
        <el-icon><Plus /></el-icon>
        新建章节
      </el-button>
    </el-form-item>

    <!-- 未分类资源提示 -->
    <el-alert
      v-if="unassignedCount > 0"
      type="warning"
      :closable="false"
      style="margin-bottom: 16px;"
    >
      <template #title>
        有 {{ unassignedCount }} 个资源未分配章节
        <el-button link type="primary" @click="showUnassignedResources">
          查看并分配
        </el-button>
      </template>
    </el-alert>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';
import { ElTreeSelect } from 'element-plus';
import { Plus } from '@element-plus/icons-vue';
import { getChapterTree } from '@/api/courseAdmin';
import type { ChapterNode } from '@/api/courseAdmin';

const props = defineProps<{
  courseId: number;
  modelValue?: number | null;
}>();

const emit = defineEmits<{
  'update:modelValue': [value: number | null];
}>();

const chapterOptions = ref<ChapterNode[]>([]);
const unassignedCount = ref(0);

const form = computed({
  get: () => ({ chapterId: props.modelValue }),
  set: (val) => emit('update:modelValue', val.chapterId)
});

// 加载章节选项
const loadChapterOptions = async () => {
  const res = await getChapterTree(props.courseId);
  if (res.code === 200) {
    chapterOptions.value = res.data;
  }
};

loadChapterOptions();
</script>
```

### 4.3 学习数据看板（CourseProgressDashboard.vue）

#### 功能特性
1. **课程学习概览**
   - 总学生数、完成人数、完成率
   - 平均学习进度、平均学习时长
   - 活跃度趋势图（ECharts）

2. **学生进度列表**
   - 表格展示：学生姓名、学号、完成率、学习时长、最后学习时间
   - 支持筛选：已完成/进行中/未开始
   - 支持排序：按进度/时长排序

3. **详细进度弹窗**
   - 点击学生查看详细进度
   - 每个资源的学习情况
   - 学习轨迹时间线

4. **数据导出**
   - 导出Excel学习报告
   - 包含所有学生的详细数据

#### 核心代码示例

```vue
<template>
  <div class="course-progress-dashboard">
    <!-- 概览卡片 -->
    <el-row :gutter="20" style="margin-bottom: 20px;">
      <el-col :span="6">
        <el-card>
          <el-statistic title="总学生数" :value="overview.totalStudents">
            <template #prefix>
              <el-icon><User /></el-icon>
            </template>
          </el-statistic>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic title="完成人数" :value="overview.completedStudents">
            <template #prefix>
              <el-icon><CircleCheck /></el-icon>
            </template>
          </el-statistic>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic
            title="平均进度"
            :value="overview.averageProgress"
            suffix="%"
          >
            <template #prefix>
              <el-icon><TrendCharts /></el-icon>
            </template>
          </el-statistic>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <el-statistic
            title="平均学习时长"
            :value="formatStudyTime(overview.averageStudyTime)"
          >
            <template #prefix>
              <el-icon><Clock /></el-icon>
            </template>
          </el-statistic>
        </el-card>
      </el-col>
    </el-row>

    <!-- 学生进度表格 -->
    <el-card>
      <template #header>
        <div style="display: flex; justify-content: space-between; align-items: center;">
          <span>学生学习进度</span>
          <el-button type="primary" @click="handleExport">
            <el-icon><Download /></el-icon>
            导出Excel
          </el-button>
        </div>
      </template>

      <el-table :data="studentList" v-loading="loading">
        <el-table-column prop="studentName" label="学生姓名" width="120" />
        <el-table-column prop="studentNo" label="学号" width="120" />
        <el-table-column label="完成率" width="200">
          <template #default="{ row }">
            <el-progress :percentage="row.completionRate" />
          </template>
        </el-table-column>
        <el-table-column label="学习时长" width="150">
          <template #default="{ row }">
            {{ formatStudyTime(row.totalStudyTime) }}
          </template>
        </el-table-column>
        <el-table-column prop="lastStudyTime" label="最后学习时间" width="180">
          <template #default="{ row }">
            {{ formatDateTime(row.lastStudyTime) }}
          </template>
        </el-table-column>
        <el-table-column label="操作" width="100" fixed="right">
          <template #default="{ row }">
            <el-button link type="primary" @click="viewDetail(row)">
              查看详情
            </el-button>
          </template>
        </el-table-column>
      </el-table>

      <el-pagination
        v-model:current-page="pagination.current"
        v-model:page-size="pagination.size"
        :total="pagination.total"
        layout="total, prev, pager, next, jumper"
        @current-change="loadData"
      />
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue';
import { User, CircleCheck, TrendCharts, Clock, Download } from '@element-plus/icons-vue';
import { getCourseProgressOverview, getCourseStudentProgress } from '@/api/courseAdmin';
import type { CourseProgressOverview, StudentProgressItem } from '@/api/courseAdmin';

const props = defineProps<{
  courseId: number;
}>();

const loading = ref(false);
const overview = ref<CourseProgressOverview>({
  totalStudents: 0,
  completedStudents: 0,
  averageProgress: 0,
  averageStudyTime: 0
});

const studentList = ref<StudentProgressItem[]>([]);
const pagination = reactive({
  current: 1,
  size: 10,
  total: 0
});

// 加载概览数据
const loadOverview = async () => {
  const res = await getCourseProgressOverview(props.courseId);
  if (res.code === 200) {
    overview.value = res.data;
  }
};

// 加载学生列表
const loadData = async () => {
  loading.value = true;
  const res = await getCourseStudentProgress(
    props.courseId,
    pagination.current,
    pagination.size
  );
  if (res.code === 200) {
    studentList.value = res.data.records;
    pagination.total = res.data.total;
  }
  loading.value = false;
};

// 格式化学习时长
const formatStudyTime = (seconds: number): string => {
  if (seconds < 60) return `${seconds}秒`;
  if (seconds < 3600) return `${Math.floor(seconds / 60)}分钟`;
  const hours = Math.floor(seconds / 3600);
  const minutes = Math.floor((seconds % 3600) / 60);
  return `${hours}小时${minutes}分钟`;
};

// 导出Excel
const handleExport = () => {
  window.open(`/api/v1/admin/course-progress/course/${props.courseId}/export`);
};

onMounted(() => {
  loadOverview();
  loadData();
});
</script>
```

---

## 五、实施计划

### Phase 1: 后端API开发（1天）

**任务清单**:
- [ ] 创建 `CourseChapterAdminController`
- [ ] 实现章节CRUD接口
- [ ] 实现批量排序接口
- [ ] 创建 `CourseProgressAdminController`
- [ ] 实现学习数据查询接口
- [ ] 实现Excel导出功能
- [ ] 编写单元测试

**关键文件**:
```
exe-backend/src/main/java/com/ice/exebackend/
├── controller/admin/
│   ├── CourseChapterAdminController.java    (新建)
│   └── CourseProgressAdminController.java   (新建)
├── dto/
│   ├── ChapterSortDTO.java                  (新建)
│   └── CourseProgressOverviewDTO.java       (新建)
└── service/
    └── impl/CourseProgressAdminServiceImpl.java (新建)
```

### Phase 2: 前端组件开发（2天）

**Day 1: 章节管理组件**
- [ ] 创建 `ChapterTreeEditor.vue`（树形编辑器）
- [ ] 创建 `ChapterFormDialog.vue`（章节表单）
- [ ] 创建 `useChapterTree.ts` Hook
- [ ] 创建 `useDragSort.ts` Hook
- [ ] 实现拖拽排序功能

**Day 2: 资源分配和学习数据**
- [ ] 创建 `ResourceAssignment.vue`（资源分配）
- [ ] 创建 `CourseProgressDashboard.vue`（学习数据）
- [ ] 创建 `api/courseAdmin.ts`（API接口）
- [ ] 集成到 `CourseManage.vue`

**关键文件**:
```
exe-frontend/src/
├── views/
│   ├── CourseManage.vue                     (重构)
│   └── course-admin/
│       ├── ChapterTreeEditor.vue            (新建 ~400行)
│       ├── ChapterFormDialog.vue            (新建 ~200行)
│       ├── ResourceAssignment.vue           (新建 ~300行)
│       └── CourseProgressDashboard.vue      (新建 ~600行)
├── hooks/
│   ├── useChapterTree.ts                    (新建)
│   └── useDragSort.ts                       (新建)
└── api/
    └── courseAdmin.ts                       (新建)
```

### Phase 3: UI/UX优化（0.5天）

- [ ] 响应式布局调整
- [ ] 动画效果优化
- [ ] 加载状态优化
- [ ] 错误处理和反馈
- [ ] 无障碍功能

### Phase 4: 测试和文档（0.5天）

- [ ] 功能测试（章节CRUD、拖拽、数据查看）
- [ ] 边界测试（空数据、大数据量）
- [ ] 跨浏览器测试
- [ ] 编写使用文档
- [ ] 创建视频演示

---

## 六、UI设计稿

### 6.1 课程管理主页面（重构后）

```
┌─────────────────────────────────────────────────────────────┐
│ 📚 课程资源中心                                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [新建课程]  [刷新]    搜索: [___________] [🔍]            │
│                                                             │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐                  │
│  │ 📘   │  │ 📗   │  │ 📙   │  │ 📕   │                  │
│  │数学  │  │语文  │  │英语  │  │物理  │                  │
│  │高一  │  │高一  │  │高二  │  │高二  │                  │
│  │[详情]│  │[详情]│  │[详情]│  │[详情]│                  │
│  └──────┘  └──────┘  └──────┘  └──────┘                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 课程详情抽屉（新增标签页）

```
┌─────────────────────────────────────────────────────────────┐
│ 《高一数学》                                          [×]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ [基本信息] [章节管理] [资源管理] [学习数据] [讨论管理]     │
│ ───────── ────────── ────────── ────────── ──────────      │
│                                                             │
│ 📖 章节管理                                                 │
│ ┌──────────────────────────────────────────────────────┐  │
│ │ [+ 添加根章节] [全部展开] [全部折叠]                   │  │
│ ├──────────────────────────────────────────────────────┤  │
│ │ 📁 第一章：集合与函数           [+] [✏️] [🗑️]         │  │
│ │   📁 1.1 集合的概念             [+] [✏️] [🗑️]         │  │
│ │     📄 视频：集合的定义.mp4           [✏️] [🗑️]       │  │
│ │     📄 课件：集合知识点.pdf           [✏️] [🗑️]       │  │
│ │   📁 1.2 集合的运算             [+] [✏️] [🗑️]         │  │
│ │ 📁 第二章：函数及其性质         [+] [✏️] [🗑️]         │  │
│ │ 📄 未分类资源 (3个)             [查看并分配]           │  │
│ └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 学习数据看板

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 学习数据分析 - 《高一数学》                        [导出] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                          │
│ │👥 45│ │✅ 32│ │📈71%│ │⏱️ 3h│                          │
│ │学生 │ │完成 │ │进度 │ │时长 │                          │
│ └─────┘ └─────┘ └─────┘ └─────┘                          │
│                                                             │
│ 学生进度列表                                                │
│ ┌──────────────────────────────────────────────────────┐  │
│ │ 姓名   学号    完成率     学习时长   最后学习时间      │  │
│ ├──────────────────────────────────────────────────────┤  │
│ │ 张三  2024001 [████████] 100%  4小时  2026-01-10     │  │
│ │ 李四  2024002 [██████  ]  75%  2小时  2026-01-09     │  │
│ │ 王五  2024003 [████    ]  50%  1小时  2026-01-08     │  │
│ └──────────────────────────────────────────────────────┘  │
│                                                             │
│                              [上一页] 1/5 [下一页]          │
└─────────────────────────────────────────────────────────────┘
```

---

## 七、技术亮点

### 7.1 拖拽排序优化
- 使用 Element Plus Tree 的原生拖拽
- 拖拽时显示实时预览
- 支持撤销/重做
- 防抖保存，避免频繁请求

### 7.2 大数据量优化
- 虚拟滚动（章节树超过100个节点）
- 懒加载子节点
- 分页加载学生数据
- 图表数据采样

### 7.3 权限控制
- 教师只能管理自己创建的课程
- 管理员可以管理所有课程
- 细粒度权限：查看/编辑/删除

### 7.4 数据导出
- Excel格式，包含所有学生数据
- 支持自定义导出字段
- 导出进度提示

---

## 八、预期成果

### 功能完整性
- ✅ 章节管理（创建/编辑/删除/排序）
- ✅ 资源分配到章节
- ✅ 拖拽排序（章节和资源）
- ✅ 学习数据查看（班级和个人）
- ✅ 数据导出（Excel）
- ✅ 未分类资源管理

### 用户体验
- 直观的树形结构管理
- 流畅的拖拽交互
- 清晰的数据可视化
- 快速的响应速度

### 系统价值
- 教师可以精细化组织课程内容
- 实时了解学生学习情况
- 数据驱动教学改进
- 提升课程质量

---

## 九、后续扩展

### 可选功能（v3.08+）
1. **课程模板** - 快速创建标准化课程结构
2. **批量导入** - Excel批量导入章节和资源
3. **版本管理** - 课程内容版本控制
4. **协作编辑** - 多教师协作管理课程
5. **AI辅助** - AI推荐章节结构、生成章节描述

---

**实施优先级**: 高
**预计工期**: 4天
**技术难度**: 中等
**业务价值**: 极高

准备好开始实施了吗？
