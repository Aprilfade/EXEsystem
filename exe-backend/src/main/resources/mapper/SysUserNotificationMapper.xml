<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ice.exebackend.mapper.SysUserNotificationMapper">

    <!-- 统计用户未读通知数量 -->
    <select id="countUnread" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM sys_user_notification
        WHERE user_id = #{userId}
          AND is_read = 0
    </select>

    <!-- 标记用户所有通知为已读 -->
    <update id="markAllAsRead">
        UPDATE sys_user_notification
        SET is_read = 1,
            read_time = NOW()
        WHERE user_id = #{userId}
          AND is_read = 0
    </update>

</mapper>
